export const TABLES = {
  USER: 'users',
  FILE_RECORD: 'file_records',
};

export const SCHEMAS = {
  PUBLIC: 'public',
  USER: 'public',
  COMMON: 'public',
};

export const DB = {
  TABLES,
  SCHEMAS,
};

export default DB;
